"""slop v3 — component-model rebuild.

This package is greenfield. It is built from the component model in the
repo-root ``DESIGN.md`` and consults the legacy ``src/slop`` package only as an
*inventory* of computations we want — never as a template for entity shape.

Interfaces first: until the ABC spine is validated against the hardest existing
rules, modules here carry signatures and docstrings only, no implementation.
"""

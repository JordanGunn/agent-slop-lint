[project]
name = "slop"
version = "3.0.0a0"
description = "Agentic code-quality linter — v3 component-model rebuild (interfaces only)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/slop"]

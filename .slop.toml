# slop — research fixture config
#
# Points at the v1.2.0 snapshot. Used as a stable known-slop corpus for
# the lexical-signal → corrective-action research line. Waivers and
# threshold tweaks are intentionally absent: signal suppression defeats
# the purpose of measuring distributions.

root = "docs/research/snapshots/v1.2.0/slop"
languages = ["python"]

[rules.hotspots]
enabled = false                 # snapshot has no git history — structurally N/A

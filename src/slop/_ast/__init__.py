"""AST-query primitives (tree-sitter-backed).

Single-purpose kernels that parse source files and run tree-sitter
queries. Includes the language-detection and parser-loading helpers.
Treat as private to slop.
"""

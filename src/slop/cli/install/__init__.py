"""``slop install`` — group for installable integrations (hook, skill).

Each installable target lives in its own module; ``cmd.py`` wires
them under the ``install`` subcommand parser.
"""
from __future__ import annotations

"""Vendored metric kernels from aux-skills.

This subpackage is a pruned copy of aux-skills
(https://github.com/JordanGunn/aux) at the time of vendoring. slop owns
the code here directly — upstream aux-skills changes do not automatically
flow in. See LICENSE in this directory for attribution and the top-level
NOTICE for citations covering the underlying research.

Treat this tree as private. Names may move, return shapes may tighten,
and the package path may be restructured without notice. External callers
should use slop's public rule interfaces (slop.rules, slop.engine)
rather than importing from here.
"""

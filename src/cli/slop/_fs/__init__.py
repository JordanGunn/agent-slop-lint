"""Filesystem-discovery primitives (fd-backed).

Single-purpose kernels that enumerate files on disk. Stable inputs and
outputs; treat as private to slop. See ``KERNELS_LICENSE`` at the slop
package root for the vendored Apache-2.0 attribution.
"""
